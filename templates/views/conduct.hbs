<!DOCTYPE html>
<html>
    <head>
        <title>Conduct</title>
    </head>
    <body>
        <header>
              {{> header}}
        </header>
        {{> conduct}}
        {{> guidelines}}
    </body>
</html>